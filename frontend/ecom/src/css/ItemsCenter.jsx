import { createTheme } from "@mui/material";

const theme = createTheme();

export const ItemCenterStyle = {
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
}